# Example stacks 

This directory contains example stacks that can be used as a reference when you want to create a stack file specific to a particular deployment. Each stack is a a complete configuration file that describes how to spin up an entire deployment of all modules included in the stack. 
